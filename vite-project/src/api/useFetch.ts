

const Urls:Record<string,string> = {
    profile:'/api/profile',
    login:'/api/login'
}

export const useFetch = (url:string)=>{
    const fetch = ()=>{
        // doing some stuff
    }
    return fetch;
}